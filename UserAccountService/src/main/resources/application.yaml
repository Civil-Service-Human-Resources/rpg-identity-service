spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cshr
    username: postgres
    password: qwerty
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: create # create schema on run
  test:
    testEmailAccount: 
  azure:
    activedirectory:
      usersURL: https://graph.microsoft.com/v1.0/%s/users
      oauthURL: https://login.microsoftonline.com/%s/oauth2/token
      native-client-id:
      web-client-id:
      web-client-secret:
      resourceURL: https://graph.microsoft.com
      tenant: rpgidentity.onmicrosoft.com
      authority: https://login.microsoftonline.com/common/
  notifyservice:
    templateid:
    notifyApiKey:
    accountEnableURL: http://localhost:7676/useraccount/enable/%s
  security:
    identity_username: searchusername
    identity_password: searchpassword
server:
  port: 7676