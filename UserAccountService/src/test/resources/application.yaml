spring:
  datasource:
    url: jdbc:h2:mem:USERS;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: create # create schema on run
  test:
    testEmailAccount:
  azure:
    activedirectory:
      usersURL: https://graph.microsoft.com/v1.0/%s/users
      oauthURL: https://login.microsoftonline.com/%s/oauth2/token
      native-client-id:
      web-client-id:
      web-client-secret:
      resourceURL: https://graph.microsoft.com
      tenant: rpgidentity.onmicrosoft.com
      authority: https://login.microsoftonline.com/common/
  notifyservice:
    templateid:
    notifyApiKey:
    accountEnableURL: http://localhost:7676/useraccount/enable/%s
  security:
    crud_username: crudusername
    crud_password: crudpassword
server:
  port: 7676